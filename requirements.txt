# Network Attacktool - 依赖文件
# Python 3.8+ 环境要求

# 核心依赖（大多数系统已内置）
# 如需额外依赖，请在此添加

# 可选依赖（增强功能）
# scapy>=2.4.0          # 网络数据包操作（如需高级功能）
# requests>=2.25.0      # HTTP请求（如需网络功能扩展）
# colorama>=0.4.4       # 跨平台颜色支持（Windows兼容性）

# 开发依赖
# pytest>=6.0.0         # 测试框架
# black>=20.8b1         # 代码格式化
# flake8>=3.8.0         # 代码检查

# 注意：本工具主要使用Python标准库，大多数功能无需额外安装